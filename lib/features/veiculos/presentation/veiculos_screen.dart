import 'package:flutter/material.dart';

class VeiculosScreen extends StatelessWidget {
  const VeiculosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Veículos'));
  }
}
